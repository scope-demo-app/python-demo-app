# Scope Agent configuration file
scope:
  instrumentation:
    enabled: true
    http:
      payload: true
      headers:
        - ot-tracer-spanid
        - ot-tracer-traceid
    db:
      statement_values: true
  metadata:
    extra: value
    more: value2
