# Scope Agent configuration file
instrumentation:
  enabled: true
  http:
    payloads: true
    headers:
      - ot-tracer-spanid
      - ot-tracer-traceid
  db:
    statement_values: true
metadata:
  extra: value
  more: value2
